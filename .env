#--------------------------------------------------
# COMMON
#--------------------------------------------------
COMPOSE_PROJECT_NAME=soclab
DOMAIN_NAME=localhost

#--------------------------------------------------
# THEHIVE - strangebee/thehive:5.3
#--------------------------------------------------
# Admin user created on first start
THEHIVE_ADMIN_EMAIL=admin@soc-lab.local
THEHIVE_ADMIN_PASSWORD=Password123!

# Play secret key (must be 64 chars for prod)
THEHIVE_SECRET_KEY=$(head /dev/urandom | tr -dc A-Za-z0-9 | head -c 64 ; echo '')

# API Key for Cortex to communicate with TheHive (Cortex needs this)
# This key will be created in TheHive for the 'cortex' user
THEHIVE_API_KEY_FOR_CORTEX=thehive-cortex-secret-key

#--------------------------------------------------
# CORTEX - thehiveproject/cortex:3.2.1
#--------------------------------------------------
# Admin user created on first start
CORTEX_ADMIN_LOGIN=admin@soc-lab.local
CORTEX_ADMIN_PASSWORD=Password123!

# API Key for TheHive to communicate with Cortex (TheHive needs this)
CORTEX_API_KEY_FOR_THEHIVE=cortex-thehive-secret-key

#--------------------------------------------------
# MISP - coolacid/misp-docker:core-latest
#--------------------------------------------------
# MISP Admin API Key to be set by init script
MISP_ADMIN_API_KEY=misp-admin-secret-key-CHANGE-ME

# MISP MySQL Database
MYSQL_DATABASE=misp
MYSQL_USER=misp
MYSQL_PASSWORD=misp-db-password
MYSQL_ROOT_PASSWORD=misp-db-root-password

#--------------------------------------------------
# MINIO - quay.io/minio/minio:latest
#--------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin-password
MINIO_BUCKET_NAME=thehive

#--------------------------------------------------
# CASSANDRA - cassandra:4.1 (for TheHive)
#--------------------------------------------------
CASSANDRA_USER=thehive
CASSANDRA_PASSWORD=thehive-db-password

#--------------------------------------------------
# ELASTICSEARCH - docker.elastic.co/elasticsearch/elasticsearch:7.17.9
#--------------------------------------------------
# Set to 'false' for single-node development cluster
ELASTIC_DISCOVERY_TYPE=single-node
# Memory allocation
ELASTIC_MEM_LIMIT=2g
